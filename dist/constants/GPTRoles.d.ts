/**
 * Enumeration representing roles for GPT entities.
 */
export declare enum GPTRoles {
    /**
     * Role representing a user entity.
     */
    USER,
    /**
     * Role representing an assistant entity.
     */
    ASSISTANT,
    /**
     * Role representing a system entity.
     */
    SYSTEM
}
